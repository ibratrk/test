import React, {createContext} from 'react'

export const MessageContext = createCpntext()

const About = () => {
  return (
    <div>
      <h1> About - useContext </h1>
    </div>
  )
}

export default About
